## Code Style Guidelines for GitHub Copilot
- Prefer single imports over wildcard imports.
- When writing tests start the tests with `should` 
- Always add javadoc comments to public methods and classes
- Don't use nested classes, make a file every time and make packages wisely named